import styled from 'styled-components';

const FormSection = styled.div`
    padding: 20px;
`;

const SectionTitle = styled.h1`
    font-size: 28px;
    margin: 0 0 10px;
    padding: 0;
`;

export { FormSection, SectionTitle };